{% extends 'base.html.twig' %}

{% block body %}

    {% if user %}
        <h1>User Profile</h1>
        <strong>Name: </strong>{{user.name}} <br/>
        <button type="button"
                onclick="location.href='{{ path('aboutdetailspage', {'name':user.name}) }}'">
            more</button>
    {% else %}
        Mava is a web app for task management and team collaboration. <br/>
    {% endif %}

{% endblock %}